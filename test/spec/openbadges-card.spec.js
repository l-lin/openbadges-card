describe('openbadgeCard', function() {
    'use strict';

    // load the directive's module
    beforeEach(module('openbadgeCard'));

    // Initialize the controller and a mock scope
    beforeEach(inject(function() {}));

    it('should test something...', function() {
        // TODO: Complete test
    });
});
